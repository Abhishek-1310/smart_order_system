{
  "name": "smart-order-system",
  "version": "1.0.0",
  "description": "Smart Order Processing System - AWS Serverless Architecture",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ../infrastructure && npm install",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:infra": "cd infrastructure && npm run build",
    "build:all": "npm run build:backend && npm run build:frontend && npm run build:infra",
    "dev:frontend": "cd frontend && npm run dev",
    "deploy": "cd infrastructure && npx cdk deploy --all",
    "test:backend": "cd backend && npm test"
  },
  "author": "Abhishek",
  "license": "MIT"
}
